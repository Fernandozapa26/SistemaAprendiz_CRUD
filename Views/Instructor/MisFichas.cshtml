@model IEnumerable<SistemaAprendices.Models.Ficha>

@{
    ViewData["Title"] = "Mis Fichas";
}

<div class="container mt-4">
    <h2>Mis Fichas Asignadas</h2>

    <table class="table table-bordered mt-3">
        <thead class="table-dark">
            <tr>
                <th>Código</th>
                <th>Programa</th>
                <th>Jornada</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var ficha in Model)
            {
                <tr>
                    <td>@ficha.Codigo</td>
                    <td>@ficha.Programa</td>
                    <td>@ficha.Jornada</td>
                    <td>
                        
                        <a asp-controller="Instructor"
                               asp-action="VerAprendices"
                               asp-route-fichaId="@ficha.Id"
                               class="btn btn-primary btn-sm">
                            Ver Aprendices
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <a href="/" class="btn btn-secondary">Volver al Inicio</a>
</div>